import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // تأكد أن الاسم هنا يطابق ملف الكود الرئيسي

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
